# ALETHEIA — дизайн-система v3

Один источник правды для всего сайта. Всё, что верстается дальше, пляшет отсюда,
а не придумывается заново в каждой секции. Составлено 12.08.2026 при переделке.

## Откуда взято

Референсы брались не по нише («AI-агентство»), а по ощущению: тёмные продуктовые сайты
с инженерной планкой и большим количеством воздуха. Ориентиры по **планке качества**,
не по внешности: Linear (ритм и типографика), Vercel (чёрный монохром и сдержанность),
Anthropic (спокойный тон без техно-агрессии).

⚠️ С референсов берём инженерную планку, а не внешность. Правило Сергея от 18.06:
«почему всё по один шаблон, у всех должна быть уникальность». ДНК ALETHEIA
(canvas-нейросеть + циан) не заменяется ничем из референсов.

## Формулировка планки от Сергея

«Красиво, стильно, дорого, богато» = **воздух и сдержанность**, безупречная типографика,
**один сильный акцент**. НЕ свалка эффектов, не золото, не блёстки. Дорогое выглядит
спокойным. Дешёвое пытается доказать, что оно дорогое.

## 1. Палитра

| Роль | Значение | Где |
|---|---|---|
| Фон | `#06060d` | вся страница, единственный фон |
| Текст основной | `#f4f6fc` | заголовки, важное |
| Текст приглушённый | `#a7aec6` | лиды, описания (7.1:1, проходит AA) |
| Текст второстепенный | `#828aa6` | подписи, сноски (≥4.6:1) |
| **Акцент** | `#22d3ee` циан | **один на весь сайт** |
| Волосяная линия | `rgba(255,255,255,.09)` | разделители секций и строк |

**Стратегия цвета: Restrained.** Акцент занимает менее 10% поверхности: кнопка, одно слово
в заголовке, точка у брендмарка, всплывающая точка в строке при наведении.

**Фиолетовый выведен из интерфейса.** Он остаётся только внутри canvas как второй цвет узлов.
Именно фиолетовые blob-градиенты давали ощущение «шаблонный AI-сайт», их больше нет.

## 2. Типографика

Одна семья, **Manrope**, три веса (400/700/800). Пара из двух семей не нужна: контраст
делаем весом и размером, а не вторым шрифтом. Manrope уже был в бренде, идентичность не ломаем.

| Уровень | Размер | Интерлиньяж | Трекинг |
|---|---|---|---|
| H1 | `clamp(2.6rem, 5.6vw, 4.5rem)` | 1.04 | -.032em |
| H2 | `clamp(1.9rem, 4vw, 3rem)` | 1.1 | -.028em |
| Крупная строка | `clamp(1.15rem, 2vw, 1.4rem)` | 1.4 | -.015em |
| Body | 18px | 1.65 | 0 |
| Подпись | 15px | 1.5 | 0 |

Правила:
- Потолок заголовка **4.5rem**, не выше. Выше — это крик, а не дизайн.
- Пол трекинга **-.04em**, мы держим -.032em: буквы не слипаются.
- `max-width` на заголовке задаётся **в ch на самом заголовке**, не на родителе.
  На родителе `ch` считается по 18px и ломает заголовок в лапшу (поймано на v3, было 6 строк).
- Строка текста 52–65ch, не шире.
- `text-wrap: balance` на заголовках, `pretty` на прозе.
- Светлый текст на тёмном получает +0.05 к интерлиньяжу: он читается легче по весу.

## 3. Сетка и ритм

- Контейнер `1180px`, поля `clamp(20px, 5vw, 40px)`.
- Шаги ритма намеренно неровные: `1rem / 2rem / 4rem / clamp(5rem,10vw,9rem)`.
  Ровная линейка читается как таблица, неровная — как дыхание.
- Между секциями `clamp(5rem,10vw,9rem)` плюс волосяная линия. Линия вместо плашки:
  она отделяет, ничего не утяжеляя.
- Первый экран ровно `100svh` и всегда с видимой кнопкой. Если кнопка ушла за сгиб,
  экран собран неправильно.

## 4. Что запрещено на этом сайте

Проверять перед каждой сдачей:

- ❌ Градиентный текст (`background-clip:text`). Акцент — цветом, вес — весом.
- ❌ Карточки-клоны: сетка одинаковых плашек «иконка + заголовок + текст».
  Вместо них строки с волосяными разделителями.
- ❌ Вложенные карточки. Всегда ошибка.
- ❌ Мелкие капслочные надписи-кикеры над каждой секцией. Это шаблон 2023 года.
- ❌ Нумерация секций 01 / 02 / 03 как декор. Цифры только там, где есть реальная
  последовательность.
- ❌ Больше одного акцентного цвета.
- ❌ Длинные тире в контенте (стандарт маркетолога №61). Двоеточие, запятая, точка.
- ❌ Скрывать контент до анимации. Reveal только усиливает уже видимое.

## 5. Движение

- Кривая одна: `cubic-bezier(.16,1,.3,1)` (ease-out-expo). Без отскоков и резинок.
- Появление **только на первом экране**, лесенкой по 80мс. Базовое состояние — видно.
  Никакой `opacity:0` в исходном CSS: если скрипт не выполнится, страница остаётся читаемой.
- Микровзаимодействия: сдвиг строки на 12px и появление точки при наведении. Всё.
- `prefers-reduced-motion` выключает анимации целиком, включая canvas.

## 6. Canvas — единственный вау-момент

Живая нейросеть на фоне: узлы, связи, импульсы, реакция на курсор и палец.
Утверждена Сергеем лично 18.06. Это подпись бренда, а не декорация.

Правила: плотность узлов от площади экрана (24–64), циан доминирует (62% узлов),
фиолетовый второстепенен, линии не ярче 0.34 альфы. Canvas **никогда** не влияет
на видимость контента и не блокирует отрисовку.

## 7. Панель Tweaks (только для подбора, не для прода)

`?tweaks=1` включает живые контролы: акцент, масштаб типографики, воздух, плотность и
яркость нейросети. Нужна, чтобы Сергей крутил вживую и выбирал, а не описывал словами.
**Перед выкаткой на прод панель отключается** (она уже скрыта без параметра в адресе).
